[package]
name = "triangle"
version = "0.1.0"
edition = "2021"

[dependencies]
bytemuck = { version = "1.21.0", features = ["derive"] }
pollster = "0.4.0"
wgpu = "24.0.0"

[lib]
crate-type = ["cdylib"]
